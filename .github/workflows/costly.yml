name: row

on:
  push:

jobs:
  oh:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        hair: ["guest", "once", "neat", "outer", "descend"]
        ball: ["refine", "coast", "seldom", "tolerate"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar Vj.jar
